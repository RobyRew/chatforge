fund=false
audit=false
engine-strict=false
# better-auth (better-call) lists zod@^4 as an *optional* peer while the rest of the monorepo
# uses zod@3 (via @hono/zod-openapi). They coexist fine; keep installs (and CI/Docker) consistent.
legacy-peer-deps=true
