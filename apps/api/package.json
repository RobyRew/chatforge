{
  "name": "@chatforge/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ChatForge backend \u2014 Hono + zod-openapi + Drizzle, RBAC admin, opt-in server-side conversion sandbox.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "clean": "rm -rf dist .turbo coverage",
    "build": "esbuild src/server.ts src/migrate.ts --bundle --platform=node --target=node22 --format=esm --outdir=dist --out-extension:.js=.mjs --sourcemap --log-level=warning --external:pg-native --external:bufferutil --external:utf-8-validate --banner:js=\"import { createRequire as __chatforgeCreateRequire } from 'node:module'; const require = __chatforgeCreateRequire(import.meta.url);\"",
    "start:prod": "node dist/server.mjs",
    "db:migrate:prod": "node dist/migrate.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1119.0",
    "@chatforge/core": "*",
    "@chatforge/types": "*",
    "@hono/node-server": "^1.13.7",
    "@hono/zod-openapi": "^0.18.3",
    "@logto/node": "^3.0.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.6.16",
    "pg": "^8.13.1",
    "ws": "^8.21.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@chatforge/crypto": "*",
    "@types/node": "^22.10.7",
    "@types/pg": "^8.11.10",
    "@types/ws": "^8.18.1",
    "drizzle-kit": "^0.31.10",
    "esbuild": "^0.25.4",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^4.1.11"
  }
}
