{
  "name": "@chatforge/types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Canonical chat model, platform/format ids, capability matrix, fidelity report, DTOs.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "echo \"(types: no tests)\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
